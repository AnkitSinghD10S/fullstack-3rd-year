<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    li{
        list-style: none;

    }
</style>
<body>
    <a href="/users/new"> new User account</a>
    <h2>users page</h2>
    <ul>
        <% Users.forEach(user => { %>
            <li><%= user.name %></li>
            <li><%= user.email %></li>
            <li><%= user.city%></li>
            <br>
            <a href="/user/<%=user.id %>">Show</a>
            <a href="/user/<%=user.id%>/edit">edit</a>
            <form action="/delete/<%=user.id%>" method="post">
                <button type="submit">delete</button>
            <br>
        <% }); %>
    </ul>
</body>
</html>